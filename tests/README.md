# PIKET web regression tests

Reproducible tests for the route, positioning, restriction, and browser-specific logic embedded in `index.html`.

Run with the bundled or system Node.js runtime:

```text
node tests/release-check.mjs
```
